const organizationId = "bUeqfMAntygtq0mjr3ji";
const siteId = "site_enuwz";

// The Ask endpoint requires an Organization ID and Site ID
const response = await fetch("https://api.gitbook.com/v1/orgs/bUeqfMAntygtq0mjr3ji/sites/site_enuwz/ask", {
  method: "POST",
  headers: {
    // Generate a personal access token in Developer settings first.
    "Authorization": "Bearer YOUR_ACCESS_TOKEN",
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    question: "How do I get started?",
    scope: {
      mode: "default"
    }
  })
});

const data = await response.json();
